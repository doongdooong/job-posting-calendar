# 채용공고 캘린더 — 개발 기록

`채용공고 캘린더` 웹앱을 만든 과정을 요청사항과 의사결정 방향 중심으로 정리한 문서.
코드 자체의 세부 구현보다는 "왜 이렇게 했는지"에 초점을 맞췄다.

---

## 1. 배경 및 목적

- **문제**: 지원하는 채용공고가 너무 많아져서 마감일·진행상황 트래킹이 힘들어짐
- **목적**: 채용공고를 캘린더 형태로 한눈에 볼 수 있는 개인용 웹앱
- **참고 사례**: 이전에 만든 `뉴스레터`(아침 브리핑) 프로젝트와 같은 방식 —
  GitHub Pages로 무료 호스팅 + 아이폰 홈 화면에 추가해서 앱처럼 사용

## 2. 최초 요구사항 (1차 요청)

| 항목 | 내용 |
|---|---|
| 메인 화면 | 캘린더(월간) 형식, 참고 이미지(Google 캘린더 스타일) |
| 추가 기능 | 우측 하단 `+` 버튼으로 공고 추가 |
| 입력 필드 | 회사명(필수) / 직무 / 분류(신입·체험형인턴·채용연계형인턴·계약직) / 시작일 / 마감일+시간 / 공고링크 / 우선순위 — 회사명 외 전부 선택 |
| 캘린더 표시 | 회사명만 노출. 우선순위 색상: 높음 `#d4edbc` · 중간 `#ffe5a0` · 낮음 `#ffcfc9`. 시작일+마감일 있으면 범위형 막대, 마감일만 있으면 당일 일정 |
| 상세보기 | 탭하면 회사명·직무·분류·마감일시·링크 표시 + 지원중/지원완료 토글 + 수정/삭제 버튼(삭제는 예/아니오 확인) |
| 필터 | 우측 상단, 지원중/지원완료 체크박스 다중 선택 |

## 3. 기술적 의사결정

### 3-1. 왜 Claude Artifact의 `artifact` 캐패빌리티를 썼나
- 이 앱은 "페이지 자체가 기록"이 되는 트래커 성격 → Artifact 플랫폼의 `artifact` capability(자가 재게시) 패턴에 정확히 부합
- 추가·수정·삭제·상태토글마다 페이지를 새 버전으로 재게시(publish) → **기기 간 데이터가 동기화**됨 (폰/PC 어디서 열어도 동일)
- `localStorage`만 쓰는 방식도 고려했지만, 참고 이미지가 폰 캘린더였고 실사용은 여러 기기에서 이뤄질 가능성이 높아 기각
- Artifact capability가 없는 환경(예: GitHub Pages 정적 호스팅)에서는 자동으로 `localStorage` 폴백 — 같은 코드가 두 배포처 모두에서 동작

### 3-2. 렌더링 방식
- 순수 vanilla JS(프레임워크 없음), 단일 HTML 파일
- 상태(`state.postings`)를 기반으로 전체 화면을 매번 다시 그리는 구조
- 페이지 자기 자신의 `<script>` 태그 내용을 `document.currentScript.outerHTML`로 캡처해두고, 재게시 시 이걸 그대로 재사용 → 로직 중복 없이 "발행용 전체 HTML"을 조립

### 3-3. 디자인
- 폰트: 제목/본문 IBM Plex Sans KR, 날짜·숫자류는 IBM Plex Mono (Google Fonts)
- 라이트/다크 테마 모두 지원 (`prefers-color-scheme` + 수동 전환 대응)
- 우선순위 색상은 사용자가 지정한 헥스값을 그대로 유지

## 4. GitHub Pages 이중 호스팅

- `webapp-hosting-guide.md` 문서(뉴스레터 프로젝트에서 썼던 방식) 참고해서 진행
- 이 환경엔 `gh` CLI가 없어서, **저장소 생성만 사용자가 GitHub 웹사이트에서 직접 수행** (Public, `job-posting-calendar`)
- 이후 클론 → `index.html` 커밋 → `git push`는 Claude가 처리 (Windows Git Credential Manager에 캐시된 자격증명 사용, 별도 로그인 불필요)
- GitHub Pages 설정(Settings → Pages → Deploy from branch → main/root)도 사용자가 직접 켬
- 결과: **Artifact 버전**(기기 간 동기화 O)과 **GitHub Pages 버전**(브라우저별 localStorage 저장, 홈 화면에 추가해서 앱처럼 사용) 두 곳에 동일한 코드를 배포

## 5. 이후 피드백 라운드 (요청 → 원인 진단 → 결정)

### 라운드 1 — 연속 막대 버그, 목록뷰 추가
- **요청**: 여러 날짜에 걸친 일정이 하루하루 끊어진 막대(bar)로 보임 → 하나의 이어진 막대로. 캘린더 외에 목록형 페이지 추가, 목록엔 회사명·직무·분류·마감일시, 우선순위는 회사명 옆 색상 원, 왼쪽엔 체크박스 대신 토글, `+` 버튼은 캘린더와 같은 위치
- **원인**: 요일 셀마다 막대 조각을 따로 그리고 CSS 마진으로 이어붙이던 방식이라 셀 경계선이 그대로 보임
- **결정**: 요일 그리드 위에 별도의 `events-overlay` 그리드 레이어를 얹고, 멀티데이 일정을 **하나의 grid-column-span 요소**로 렌더링 (진짜 하나의 이어진 도형)
- 목록뷰는 체크리스트 앱(첨부 예시) 스타일 참고해 토글 + 우선순위 컬러 도트로 구성

### 라운드 2 — 캘린더 칸 고정 높이, 폼 필드 정리
- **요청**: 회사명이 더 길게 보이게(폰트/여백 축소), 캘린더 칸 높이를 일정 개수와 무관하게 고정. 시작일/마감일 겹침 버그, 시작 시간 필드 추가, 개인 메모 필드, 목록뷰 우선순위 원 색상이 칙칙함
- **결정**:
  - 주(week) 행 높이를 고정값(89px)으로, 넘치는 일정은 항상 `+N건` 칩 처리 (최대 표시 라인 4→3 + 오버플로 전용 칸 1개 예약)
  - 우선순위 원 색상을 캘린더와 **동일한 파스텔 배경색**으로 (기존엔 어두운 텍스트색을 써서 칙칙해 보였음) + 얇은 테두리로 흰 배경에서도 구분되게

### 라운드 3 — iOS Safari 겹침/확대 버그, 정렬+필터 통합
- **요청**: 시작일-마감일 입력칸이 여전히 겹침. 입력할 때마다 화면이 확대됨. 목록뷰의 필터 아이콘 자리에 "정렬" 기능(우선순위 높은순/마감일 임박순, **클릭 순서로 1차·2차 정렬**) + 필터를 그 안에 포함
- **원인 진단**:
  - 겹침: 네이티브 `<input type=date>`의 최소 렌더링 폭이 CSS 그리드의 좁은 컬럼(1fr)보다 커서 발생 (`min-width:0` 부재)
  - 확대: iOS Safari는 입력 필드 `font-size`가 16px 미만이면 포커스 시 자동 확대함
- **결정**:
  - 2열 그리드(날짜|날짜) 배치를 포기하고, 시작/마감 각각 **날짜+시간을 세로로 쌓는 구조**로 변경 → 근본적으로 나란히 배치하지 않아 겹침 원인 제거
  - 모든 입력 필드 `font-size: 16px`로 통일 (확대 방지)
  - 정렬 로직: 클릭한 순서를 배열로 저장(`sortOrder`), 첫 번째 클릭 기준=1차(외부) 정렬, 두 번째=2차(내부) 정렬 — 사용자가 준 예시 두 케이스로 직접 검증

### 라운드 4 — 입력 필드 단순화, 필터 버튼 제거
- **요청**: 박스로 감싼 날짜/시간 필드가 못생김 → 캘린더·시계 아이콘만 남기기. 목록뷰에 정렬(필터 포함) 버튼이 생겼으니 캘린더뷰의 별도 "필터" 버튼은 불필요 → 제거
- **결정**:
  - 라벨/박스 제거, `input-icon-wrap`(아이콘 오버레이) 방식으로 단순화
  - 캘린더뷰 상단의 "필터" 버튼 완전히 제거. 필터 상태는 전역으로 공유되므로 목록뷰의 "정렬" 패널에서만 조작해도 캘린더뷰에 그대로 반영됨 (컨트롤 중복 제거)

### 라운드 5 — 입력값 줄바꿈
- **요청**: 날짜/시간 칸 안에서 값이 줄바꿈되어 보임 (예: `2026. 9.` / `19.`처럼 두 줄로 깨짐)
- **원인**: 입력칸 너비를 `ch` 단위로 좁게 계산했는데, iOS 네이티브 컨트롤이 렌더링하는 실제 값 텍스트(예: 시간의 "오후 10:13")는 페이지의 CSS 폰트를 따르지 않아 예상보다 훨씬 넓었음
- **결정**: `ch` 계산을 버리고 고정 px로 여유 있게 확대 (날짜 172px · 시간 136px), 매우 좁은 화면 대비 `flex-wrap`으로 안전장치 추가

### 라운드 6 — 날짜 필터(토글) 추가
- **요청**: "마감 공고 포함/미포함", "모집 전 공고 포함/미포함" 필터 추가. UI를 토글로 할지 체크박스로 할지 먼저 제안하라는 요청
- **판단 기준 제안**: 기존 상태 필터(지원중/지원완료)는 "여러 카테고리 중 고르는" 다중 선택이라 체크박스가 맞지만, 이번 두 옵션은 각각 **독립적인 단일 on/off 설정**이라 이미 앱에 있는 지원상태 토글과 성격이 같음 → **토글을 추천**, 사용자 승인 후 진행
- **로직**:
  - "마감 지난 공고" 끄면: 마감일+시간이 **현재 시각**보다 과거인 공고를 숨김. 시간이 없으면 그날 23:59로 간주(당일 마감은 그날 안엔 계속 보임)
  - "모집 전 공고" 끄면: 시작일+시간이 현재 시각보다 미래인 공고를 숨김. 시간이 없으면 그날 00:00으로 간주(당일 시작은 바로 표시)
  - 둘 다 기본값 켜짐(=전체 표시, 기존 동작과 동일)
  - 캘린더/목록 양쪽에 공통 적용(전역 필터 상태 공유)

## 6. 검증 방식

매 변경마다 로컬 정적 서버(Python `http.server`)로 브라우저 자동화 도구를 이용해:
- 실제 DOM 좌표를 측정해 겹침 여부(`overlaps()`) 직접 계산
- 정렬 예시 케이스를 그대로 재현해 결과 배열 비교
- 날짜 필터는 오늘 기준 과거/오늘-이른시각/오늘-늦은시각/미래 등 8가지 케이스를 만들어 기대 결과와 일일이 대조
- 매번 확인 후에만 Artifact 재게시 + GitHub 커밋·푸시 진행

## 7. 최종 결과물

- **Artifact** (기기 간 자동 동기화): https://claude.ai/code/artifact/f2b1b5ab-0376-4536-ab74-1f44701888ef
- **GitHub Pages** (브라우저별 저장, 홈 화면에 추가해서 앱처럼 사용): https://doongdooong.github.io/job-posting-calendar/
- **저장소**: `doongdooong/job-posting-calendar`
- 파일 구조: `index.html` 단일 파일 (마크업+스타일+로직), `README.md`, `DEVELOPMENT.md`(이 문서)

## 8. 데이터 저장 방식 요약

| 배포처 | 저장 위치 | 동기화 |
|---|---|---|
| Artifact | 플랫폼이 관리하는 문서 버전(발행할 때마다 갱신) | 기기/브라우저 무관하게 동일하게 보임 |
| GitHub Pages | 그 브라우저의 `localStorage` | 기기·브라우저마다 따로 저장됨(동기화 안 됨) |

여러 기기에서 GitHub Pages 버전도 동일하게 보이게 하려면, `뉴스레터` 프로젝트처럼 데이터를 저장소 파일(JSON)로 커밋하고 `fetch`로 읽어오는 구조로 바꿔야 함 — 필요해지면 추가 논의.
